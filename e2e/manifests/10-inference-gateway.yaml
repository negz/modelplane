# The front door. It runs on the workload InferenceCluster, which already runs
# Envoy Gateway and Envoy AI Gateway for its own cluster gateway, so nothing is
# installed on the control plane and the two clusters no longer race for the
# Gateway API CRDs.
#
# Co-locating the fleet gateway with the models it serves is a supported shape,
# and the one this test uses: the workload cluster hosts both gateways, each with
# its own LoadBalancer address from the MetalLB run.sh installs there.
#
# No hostname, TLS or caller keys. That's the getting-started shape: the gateway
# answers on its address over plain HTTP and authenticates nobody. Caller
# authentication is exercised separately, since it needs Secrets.
apiVersion: modelplane.ai/v1alpha1
kind: InferenceGateway
metadata:
  name: local
spec:
  clusterName: local
